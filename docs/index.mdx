---
title: Getting Started
---

### 🚧 TODO 🚧
- Citations in embed
- Make embed grow to fit preview
- Indicate that examples are editable

# WebTeX

Web$\TeX$ is tool that helps with scientific and technical writing. It builds on Markdown, which lets you do the basic formatting that you expect from any writing app. Web$\TeX$ extends Markdown by adding support for math expressions, citations, and cross-referencing.

## Basic formatting

To make things like headings, **bold** text, _italic_ text, and [links](#), you can use Markdown. You may have used Markdown before on sites like Reddit or GitHub.

It works like this:

<Embed defaultValue=
{`**Bold**

_Italic_

[Link](https://webtex.vercel.app/)

---

## Section

### Subsection

---

- Apples
- Oranges
- Pears

1. Eat
2. Sleep
3. Write
4. Repeat

---

![This is a figure.](/nickelback.jpg)

---

> This is a quote.

---

Inline \`code\` with backticks

\`\`\`
Code blocks with triple backticks
\`\`\``}
/>

In the example above, two things are different from most other Markdown tools:

1. Section headings are automatically numbered
2. Images are given a caption, and they are automatically numbered

Other than that, Web$\TeX$ follows the [CommonMark spec](https://commonmark.org/).
{/* NOTE: well, I'm not sure because we're doing a bunch of other things */}

<Callout>Headings with one `#` are not numbered, so you can use these as the title of the document.</Callout>

<Callout>If you don't want to use captions or automatic numbering, check out the section on [HTML](#html).</Callout>

## Math

Math works just like in $\LaTeX$, by wrapping it in $-signs:

<Embed defaultValue=
{`Inline math: $e^{i\\pi} = -1$

And here is a math block:

$$
\\binom{n}{k} = \\frac{n!}{k!(n-k)!}
$$`}
/>

Many $\TeX$ functions are supported, including macros using `\newcommand` etc. For a full list of supported functions, refer to the [KaTeX docs](https://katex.org/docs/supported.html).

## Citations

TODO

## Cross-referencing

Figures and sections can be referenced using the `:ref[id]` command, where `id` is the ID of the element you want to reference. Before something can be referenced, it must have an ID. You can give something an ID using the `:id[your-id]` command.

For example, this is how you reference a figure:

<Embed defaultValue=
{`![Look at this graph!](/nickelback.jpg) :id[graph]

---

Now :ref[graph] is a reference to the figure.`}
/>

Referencing sections is even easier, because you don't have to give them an ID unless you want to. Check this out:

<Embed defaultValue=
{`## A section :id[first-section]

Some text.

## Another section

More text.

---

:ref[first-section] works.

And :ref[another-section] works too!`}
/>

<Callout variant="warning">Be careful when referencing sections without giving them an ID. When you change the title of the section, the reference will break.</Callout>

<Callout>You can find the ID of any section by hovering over the heading in the preview.</Callout>

## HTML

A great feature of Markdown is that you can include any HTML you want.

Just look at the possibilities:

<Embed defaultValue=
{`You can use many HTML elements, for example to create <mark>marked text</mark>.

---

You can do custom styling with CSS:

<p style="color: blue; text-align: center;">I'm blue, da ba dee da ba daa</p>

---

And this is how you insert an image without a caption:

<img src="/nickelback.jpg">

Or a heading without numbering:

<h2>Don't count on me!</h2>
`}
/>

<Callout>There are a lot of HTML elements, take a look at [this list](https://developer.mozilla.org/en-US/docs/Web/HTML/Element#inline_text_semantics) for some useful ones.</Callout>
